{% extends 'base.html.twig' %}

{% block title %}Reservez vos billets{% endblock %}

{% block body %}



<div class="input-group-addon">
    {# {{ dump(app.request.get('date')) }} #}
    {# {{ dump(app.request.get('plage')) }} #}
    {# {{ dump(app.user) }} #}
    {# {{ dump(app.user.email) }} #}
    {{ form_start(BilleterieForm) }}
        {{ form_row(BilleterieForm.lastname) }}
        {{ form_row(BilleterieForm.firstname) }}
        {{ form_row(BilleterieForm.phone) }}
        {# {{ form_row(BilleterieForm.email) }} #}



        {# {% if app.request.get('artist') != null %}
            {{ form_row(BilleterieForm.artist, {
                                disabled: true
            }) }}
         {% else %}
            {{ form_row(BilleterieForm.artist) }}
        {% endif %}

        {% if app.request.get('date') != null %}
            {{ form_row(BilleterieForm.date, {
                                disabled: true
            }) }}
        {% else %}
            {{ form_row(BilleterieForm.date) }}
        {% endif %}

        {% if app.request.get('plage') != null %}
            {{ form_row(BilleterieForm.plage, {
                                disabled: true
            }) }}
        {% else %}
            {{ form_row(BilleterieForm.plage) }}
        {% endif %}

        {{ form_row(BilleterieForm.nbTickets) }} #}

        <button type="submit" class="mb-3 mt-3 btn btn-lg btn-primary">Envoyer</button>

    {{ form_end(BilleterieForm) }}
</div>




{% endblock %}
