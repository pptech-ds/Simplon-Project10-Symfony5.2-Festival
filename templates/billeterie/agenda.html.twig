{% extends 'base.html.twig' %}

{% block title %}Hello BilleterieController!{% endblock %}

{% block body %}




<table class="table table-hover mb-5 mt-4">
    <thead>
        <tr hidden>
        <th scope="col">Date</th>
        <th scope="col">Time</th>
        <th scope="col">Artist</th>
        <th scope="col">Reservation</th>
        </tr>
    </thead>
    <tbody>
        {% set i = 0 %}
        {% for date in agenda.dates %}
            {% for plage in agenda.plages %}
                <tr class="table-active" style="border: 1px solid blue">
                    <td class="text-left">{{ date }}</td>
                    <td class="text-center">{{ plage }}</td>
                    <td class="text-center">{{ artists[i].name }}</td>
                    <td class="text-center"><a href="{{path('billeterie_form', {'date':date, 'plage':plage, 'artist':artists[i].name })}}" type="button" class="btn btn-primary">Réserver une place</a></td>
                </tr>
                {% set i = i + 1  %}
            {% endfor %}
        {% endfor %}
        

    </tbody>
</table>




{% endblock %}
